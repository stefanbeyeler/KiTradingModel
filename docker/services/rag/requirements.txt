# RAG Service Requirements

# Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.0.0

# HTTP Client
httpx>=0.25.0
aiohttp>=3.9.0

# ML/Deep Learning
torch>=2.0.0
numpy>=1.24.0

# Embedding Models
# Note: Using older versions compatible with PyTorch 2.1 in base image
sentence-transformers>=2.2.2,<3.0.0
transformers>=4.30.0,<4.42.0

# Vector Store (faiss-cpu installed separately in Dockerfile)
# FAISS uses embeddings from sentence-transformers

# Utilities
loguru>=0.7.0
python-dotenv>=1.0.0
redis>=5.0.0
