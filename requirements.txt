# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# LLM Integration (Ollama for Llama 3.1 70B)
ollama>=0.1.6

# PyTorch with CUDA support (for RTX 3070)
# Install manually: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0

# RAG System (FAISS with GPU support)
# For GPU: pip install faiss-gpu (requires CUDA)
# For CPU fallback: pip install faiss-cpu
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2

# Database & API Client
httpx>=0.25.0
sqlalchemy[asyncio]>=2.0.23
asyncpg>=0.29.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.0
tenacity>=8.2.0

# NHITS Neural Forecasting (Custom PyTorch implementation)
# Note: Uses PyTorch directly instead of NeuralForecast due to Ray dependency issues on Windows
# torch is already installed above
